#!/bin/sh
# SPDX-License-Identifier: AGPL-3.0-or-later
#
# AI-GENERATED CODE NOTICE:
# This source code was generated by OpenAI ChatGPT for the
# urbackup-proxmox-agent project. It has NOT been reviewed by a human
# software developer. Use only after testing in a non-production environment.
#
# UrBackup calls create_volume_snapshot with:
#   $1 snapshot-set GUID
#   $2 volume/device
#   $3 requested directory/name
#   $4 original target/device
#   $5 optional virtual client name
#
# For image backups, $4 is the original block device that must be snapshotted.

set -eu
HOOK=/usr/local/lib/urbackup-proxmox-agent/pve_volume_snapshot.py

if [ "$#" -lt 4 ]; then
    echo "ERROR: expected at least 4 arguments" >&2
    exit 2
fi

exec "$HOOK" create "$1" "$4"
